# openapi-typescript declares a peer range of typescript ^5 and this project pins ^6.
# The range is stale on a dev-only tool, so installs are allowed past it. Without this,
# npm install and any tool that shells out to it (shadcn, for one) fail on a clean checkout.
legacy-peer-deps=true
