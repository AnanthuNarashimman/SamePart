# Bare `pytest` from the repository root collects the suite and nothing else.
# Before this it collected zero tests from the project and thousands from .venv.
[pytest]
testpaths = backend/tests
pythonpath = backend
addopts = -q --strict-markers
