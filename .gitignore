__pycache__/
*.pyc
.venv/
node_modules/

# Secrets. Never commit.
.env

# Databases and generated data. Rebuild with: python -m samepart.cli seed
samepart.db
data/

# The UNSPSC codeset lives under data/ and is licensed PERSONAL USE ONLY.
# It forbids redistribution, so it must never be committed.

# Local working notes and the original brief
notes/
SamePart_Project_Plan*.docx

# Local skills CLI installs (npx skills add ...) — machine-local, not app code
.agents/
.claude/
skills-lock.json
